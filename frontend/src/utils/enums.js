export const StatusIntegracao = Object.freeze({
    EXCLUIDO: 0,
    ATIVO: 1,
    PENDENTE: 2,
    INATIVO: 3
});

export const Marketplace = Object.freeze({
    MERCADO_LIVRE: 1,
});

export const tipoMovimentacaoEstoque = Object.freeze({
  ENTRADA: 1,
  SAIDA: 2
});

export const produtoStatus = Object.freeze({
  ATIVO: 0,
  INATIVO: 1,
  EXCLUIDO: 2
});
